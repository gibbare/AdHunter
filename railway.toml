[deploy]
# Cron-schema hanteras av GitHub Actions (monitor.yml) för beständig seen_ads.json
# Railway används inte längre som primär schemaläggare
startCommand = "echo 'Schemaläggning sker via GitHub Actions'"

[build]
builder = "nixpacks"
